[tool.poetry]
name = "test-project"
version = "0.1.0"
description = ""
authors = ["Anton Vopilov"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
langchain = "0.1.14"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
